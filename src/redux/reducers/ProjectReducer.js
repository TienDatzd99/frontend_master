const initialState = {
    projectEditList:{
        "id":1,
        "projectName": "string",
        "creator": 0,
        "description": "string",
        "categoryId": "string",
      
    }
}

export const ProjectReducer = (state = initialState, { type, payload }) => {
  switch (type) {

  

  default:
    return state
  }
}
